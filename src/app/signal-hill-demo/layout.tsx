export const metadata = {
	title: "Signal Hill Interactive Map — Prototype",
	robots: { index: false, follow: false },
};

export default function SignalHillDemoLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
